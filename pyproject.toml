[project]
name = "j-agent"
version = "0.1.0"
description = "Minimal autonomous generalist agent loop"
requires-python = ">=3.11"
dependencies = ["openai>=1.40.0"]

[project.scripts]
j-agent = "j_agent:main"
