# rev105 — point/pointwise stabilizers and right-coset membership

Root remains **NOT_AGI**.

Resolved permutation-group child `...c3c3c2b` at `solved_v0_1`.

Using the rev104 Schreier transversals, arbitrary point stabilizer generators are derived by Schreier's lemma. Repeating this operation yields pointwise stabilizers for arbitrary ordered or unordered point sets while preserving already-fixed points. A concrete right-coset representation was also added with explicit membership reduction back to subgroup membership through the representative inverse.

One initial repository write for this file was blocked by the connector and produced no artifact; the operation was retried in a smaller write and only the successful committed version is counted.

In-session validation:

- **300 random generated groups of degree 1–6**: the subgroup generated by the computed point-stabilizer generators exactly equaled the independently enumerated set of group elements fixing the selected point. Random pointwise stabilizers also had the exact exhaustive order and membership.
- **150 random coset cases**: sampled permutation membership in the represented right coset exactly matched explicitly materializing `{representative * h : h in H}` from the exhaustive subgroup.

The remaining permutation-group child is `...c3c3c2c`: set/string stabilizers, transporters/coset intersection, and scalable divide-and-conquer restrictions required by the higher-level isomorphism recursion.

Active problem count remains **491**, below prediction **512**.
