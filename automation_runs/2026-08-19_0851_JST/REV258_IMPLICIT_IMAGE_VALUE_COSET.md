# AGI-GI rev258: implicit image/value right-coset intersection

## Solved child

Given rev257's exact implicit unary/binary relation-image action, intersect its image group with the complete feature-value-preserving transporter set without enumerating the image group.

The implementation converts equal feature values into an ordered partition of the faithful auxiliary domain and reuses the exact bounded canonical partition transporter over the implicit Schreier chain.  A successful source-to-target transporter is combined with the conjugated target feature stabilizer to reconstruct the complete result in the repository `RightCoset` convention.

Resource exhaustion of the partition-state orbit is fail closed.  Feature-inventory mismatch and absence of a partition transporter are exact-empty outcomes.

## Strict boundary

This revision solves rev257 child (2) only.  It does not yet lift the auxiliary right coset through the paired action to the original domain, nor does it certify the original-root quasipolynomial envelope.  CRX1, GI, and AGI remain unresolved; state remains `NOT_AGI`.
