# AGI-GI rev1700 — recursive Johnson-ground child-instance execution binding

## Scope

This leaf owns only `w1r-h6/corrected-split-or-johnson/larger-ground-recursive-child-instance-execution-binding` under claim `chatgpt-session-j-rev1700-soj-recursive-child-instance-execution-20260822T165000JST-2d91c7e4`.

The earlier same-session rev1500 attempt was retired as soon as an independently owned, earlier rev1500 block-action branch became visible. rev1700 is the collision-free replacement. It is file-disjoint from that rev1500/rev1600 work, rev1400 recursive-production proof-DAG composition, rev1300 lineage closure, rev293 result lift, rev287 relational-reduction construction, homogeneous-block work, CRX scopes, `MAIN.md`, and shared S1/proof-DAG/recurrence/coordination implementation.

## Gap closed

rev1400 can bind an already-supplied exact `ProofCarryingCoset` to public recursive-production lineage and result-lift evidence, but it intentionally does not run recursive String Isomorphism itself. Consequently, a concrete child proof can be checked for exactness, recurrence measures and result-coset agreement without that adapter independently proving that the proof was produced by the main S1 dispatcher on the exact induced Johnson-ground group and exact child source/target instance named by the caller.

rev1700 closes only that execution-authenticity boundary. It:

1. accepts a rev287-shaped Johnson-ground reduction only after an explicit independent replay gate and revalidates its strict theorem flags, `C(v,k) -> v` shrink, complete canonical `J(v,k)` vertex family, reduction digest format and induced ground permutations;
2. reconstructs the exact represented child group from the retained induced-ground generators without importing rev287 branch-only implementation;
3. accepts one explicit caller-supplied source/target string on that ground and executes the main-integrated `s1_string_isomorphism_v4` on that exact group/string/root/resource instance;
4. rebuilds the complete expected `S1ProofIdentity` and requires the executed proof to validate against that identity, including deterministic Schreier-chain representation, oriented strings, original root, recursion depth and every dispatcher resource gate;
5. accepts only exact, canonical, locally cost-certified S1 executions and fail-closes on unresolved S1 outcomes or unstable/opaque instance values;
6. converts the exact executed coset into the public rev293 child-result field/hash shape, rechecks ambient membership and exact subgroup reconstruction, and binds the full execution instance plus result identity into a separate deterministic `child_instance_identity`.

## Deliberate semantic boundary

The critical remaining theorem is not hidden. A colored relation on the `C(v,k)` Johnson vertices is not automatically an ordinary string on the `v` ground points. rev1700 therefore does **not** infer or manufacture the child source/target strings from the parent relation, and every successful result carries `parent_child_semantic_transport_certified = False`.

This means rev1700 proves: “this explicit child instance was really executed by S1 and its exact result is bound to that execution.” It does **not** prove: “this child instance is the mathematically correct recursive image of the parent Johnson relation.” A separate constructive parent-to-child semantic reduction is still required before corrected Split-or-Johnson production can close.

The public rev287 certificate also does not retain every field used inside its own reduction-identity payload, so rev1700 follows the existing fail-closed pattern and requires the caller to assert that rev287-style evidence was independently replayed against its original construction inputs rather than pretending to self-replay unavailable data.

## Verification

The dedicated smoke compiles the rev1700 module/test, runs focused exact-success and fail-closed regressions, rejects imports of rev287/rev293/rev1300/rev1400 branch-only implementation modules, enforces the six reserved paths, previews canonical `attempt_solution` and `publish` phase admission from the main-integrated generator, and on this branch only materializes those canonical evidence records after a green push smoke.

Focused coverage includes exact nonempty and exact-empty S1 execution, deterministic execution identity, resource-identity drift, strict independent-replay gating, malformed theorem flags/dimensions/permutations/Johnson families, wrong child string length, opaque values, invalid root/resource gates, malformed reduction digests, and the invariant that semantic parent-to-child transport is never fabricated.

## Strict boundary

This revision does not import sibling branch-only implementations, discover a Johnson embedding, construct the parent-to-child colored relational reduction, execute rev1400 composition, modify shared S1/proof-DAG/recurrence code, merge sibling PRs, close corrected Split-or-Johnson, prove quasipolynomial GI, or establish AGI. State remains `NOT_AGI`.
