# AGI eval v1 CI probe

This branch exists only to trigger a pull-request workflow run so the final-mode container mechanics can be observed through GitHub Actions. It is not AGI evidence and should not be merged merely because the toy fixture passes.
