name: rev202 twin cell transport smoke

on:
  pull_request:
    paths:
      - 'automation_runs/2026-08-19_0851_JST/bipartite_twin_cell_transport_coset_v1.py'
      - 'automation_runs/2026-08-19_0851_JST/test_bipartite_twin_cell_transport_coset_rev202.py'
      - '.github/workflows/rev202-twin-cell-transport-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-rev202:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: automation_runs/2026-08-19_0851_JST
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pytest
      - name: Validate complete internal twin-cell transport coset
        run: python -m pytest -q test_bipartite_twin_cell_transport_coset_rev202.py
