# Rev284 — corrected SOJ explicit-Johnson terminal admission

## Scope

Rev284 closes one narrow caller boundary left explicit by rev283: a corrected Split-or-Johnson **explicit Johnson embedding** may be offered to the already-main-integrated bounded primitive-Johnson exact String-Isomorphism terminal, but the structural label alone is never promoted to an exact SI result.

This revision is additive and owns only `w1r-h6/corrected-split-or-johnson/explicit-johnson-embedding-terminal-admission`. It does not import the active rev281 transition module from PR #226, does not import rev283 from PR #228, and does not import the active rev264 proof-DAG consumer. The shared primitive-Johnson terminal and immutable group/value freezing helpers are already on `main` and are consumed unchanged.

## Fail-closed admission contract

The adapter independently revalidates the public rev281 transition shape and requires:

- exact status `certified_corrected_soj_explicit_johnson_embedding` and kind `johnson_embedding`;
- theorem-input, canonicality, exactness, progress, and finite bounded multiplicative-cost gates;
- the complete caller-supplied injective `k`-subset embedding and every unordered Johnson pair distance `k-|A∩B|`;
- exact agreement between the certified vertex count and ambient group degree;
- preservation of the supplied Johnson pair relation by every original ambient generator;
- a separately certified transition-cost bound;
- replay-stable group, embedding, pair-relation, source/target-string, root, and terminal-resource identity; and
- independent main-terminal recognition of the **same** `J(v,k)` parameters followed by an exact nonempty or exact-empty bounded-ground terminal result.

Opaque value identities, malformed/tampered relation evidence, ambient-action incompatibility, resource-cap exit, parameter disagreement, uncertified local work, or a configured root-envelope overflow all remain nonexact/fail-closed.

## Resource and recursion boundary

The adapter charges `max(0, log2(max_multiplicative_cost))` in addition to the main terminal's mechanically certified local work and checks that sum against a caller-configurable root-local quasipolynomial envelope. This is deliberately **not** a new recurrence edge. Rev283 correctly admits only the constant-factor small-part transition as `aux_shrink`; an explicit Johnson embedding is structural evidence for an exact Johnson caller, not proof of primary/auxiliary measure shrink.

If the independently recognized Johnson ground exceeds the main terminal's explicit/polylogarithmic window, rev284 returns `corrected_soj_johnson_requires_recursive_ground_handling`. Larger-ground Johnson recursion, production caller wiring, full corrected Split-or-Johnson closure, global SI/GI, and AGI remain unresolved.

## Parallel coordination

Durable claim: `chatgpt-session-j-rev284-soj-johnson-terminal-admission-20260822T104315JST-9f17a7e6`.

Branch: `agi-gi-rev284-soj-johnson-terminal-admission-session-20260822-1043`.

The canonical phase-admission generator remains blocked by the independently owned legacy/non-schema-v2 rev275 registry record. Rev284 reserves its exact `attempt_solution` evidence path but does not fabricate admission evidence and does not normalize, edit, delete, rerun, cancel, close, rebase, merge, or otherwise mutate sibling work.

AGI state: `NOT_AGI`.
