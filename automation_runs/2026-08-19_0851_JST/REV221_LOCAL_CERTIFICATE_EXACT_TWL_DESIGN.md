# AGI-GI rev221 local-certificate relation to exact t-WL Design boundary

## Selected leaf, attempt, and decomposition

The selected unresolved leaf was the remaining CRX2 canonical local-certificate /
Design-escalation boundary.  Direct inspection found that
`aggregate_fullness_relation` already emits one Boolean color for every
lexicographically ordered `t`-subset, while `stable_colored_subset_twl` consumes
exactly that complete order.  The missing edge was not another relation encoding:
it was the original-action lift from that relation through theorem-gated exact
Design branching and back to full-string isomorphism.

The attempt does not solve the parent at theorem scale.  The existing leaf is
therefore replaced by three integrating children:

1. connect the exact bounded aggregate relation to exact standard t-WL, Design
   witness branching, original-domain tuple transport and full-string SI;
2. replace the current global exact string-stabilizer aggregate with the
   theorem-scale local-certificate comparison/aggregation construction;
3. connect the resulting theorem-scale quotient action to the execution-linked
   recurrence/proof-DAG envelope at every recursive consumer.

rev221 solves child 1 only.  Children 2 and 3 remain unresolved.  Replacing one
leaf by its parent and three explicit children increases the effective problem
count from **519 to 522**; forecast remains **576**.  Actual count is below the
forecast, so the mandatory over-count traversal does not fire and no child was
suppressed.

## Exact adapter

`local_certificate_exact_twl_design_string_isomorphism` recomputes both aggregate
relations from the full source and target strings.  It accepts them only when
every `t`-subset appears exactly once in canonical combination order.  Every
original group generator is paired with its induced block permutation, without a
complement bit.  The existing exact t-WL Design composer then:

- checks the theorem parameter and exact symmetry-defect gates;
- exhausts the complete first successful individualization level;
- certifies the explicit branch charge;
- transports every tuple pair through the original group action;
- intersects every surviving candidate with the original full strings; and
- reconstructs the exact complete union only when all children are exact.

The primary-source audit also found that the pre-existing theorem gate accepted
`alpha>=1/2` and `k<=n/2`.  The cited Extended Design Lemma requires
`3/4<=alpha<1` and `2<=k<=n/4`.  rev221 corrects that gate.  Fano (`n=7,k=3`)
and Cycle5 (`n=5,k=2`) remain useful bounded WL examples but are no longer called
theorem-faithful; theorem-path regressions now use Cycle8/Cycle11 and homogeneous
`n=12,k=3` inputs inside the actual parameter window.

An aggregation cap, malformed/incomplete relation, upstream significant split,
failed theorem gate, or incomplete downstream branch remains fail closed.  The
adapter never labels the existing significant-split path as Design progress.

The local environment did not contain pytest, so every zero-argument regression
in the new adapter, aggregate relation, exact t-WL, exact Design composer, and
relation-wiring modules was invoked directly under the module path: **19 passed**.
The changed modules also passed `py_compile`.  Repository CI remains the
authoritative clean pytest gate.

The first clean CI attempt then exposed seven historical integration regressions:
rev205--rev218 fixtures still expected Cycle5 (`n=5,k=2`) to enter the Design
theorem path.  They were not repaired by weakening the corrected gate.  The
ambient transport, parent-union, recurrence, polynomial accounting, and
execution-linked proof regressions now use theorem-valid Cycle11 fixtures, plus
an `n=8,k=2` disconnected-four-cycle split.  The exact parent fixture has parent
degree 22, auxiliary degree 143, cyclic order 11, and an explicit fail-closed
auxiliary cap of 142.  All **23** affected cross-revision tests passed by direct
invocation (including monkeypatch-equivalent restoration), and both the rev221
smoke gate and the general validation gate now execute the whole repaired chain.
Together with the adapter tests above, the expanded direct integration run passed
**42 tests** before publication.

## Existing-world inclusion audit

- Root/CRX2: Babai's quasipolynomial graph-isomorphism framework supplies the
  local-certificates and Split-or-Johnson context
  (<https://arxiv.org/abs/1512.03547>).
- Child 1: the Extended Design Lemma's complete colored k-ary relational input,
  k-WL stabilization, canonical partition/Johnson alternatives, and
  `n^{O(k+log n)}` boundary are contained as explicit mechanical gates
  (<https://arxiv.org/html/1909.10260v1>).
- Child 2: the same local-certificates literature is the required existing-world
  construction to contain; global automorphism enumeration is recorded only as a
  bounded oracle and is not treated as its theorem-scale implementation.
- Child 3: existing Schreier action/preimage machinery and rev217--rev220 shared
  action, execution identity, recurrence, and proof-DAG evidence are reused rather
  than duplicating quotient lifting or inferring complexity from exactness.

This audit reaches the upper CRX2 parent as well as the new leaves.  It does not
justify deleting either unresolved child: the repository has no execution-linked
evidence that the theorem-scale local comparison is implemented or that all of
its recursive consumers satisfy the original-root envelope.

## Claim boundary

The exact small-instance connection is implemented and locally exercised.  The
current aggregate still computes a global exact string stabilizer and can fail its
configured caps; it is not Babai's theorem-scale local-certificate aggregation.
Corrected Split-or-Johnson, complete W1R-H6, global quasipolynomial SI,
independent generality/performance/autonomy evidence, practical product delivery,
and AGI are not established.  State: `NOT_AGI`.
