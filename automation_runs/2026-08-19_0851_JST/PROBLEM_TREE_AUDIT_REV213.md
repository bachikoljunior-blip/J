# AGI-GI rev213 problem-tree audit

## Count and trigger

The inherited forecast/effective count remains **512/512**. No independently observed non-replaced node above that baseline is introduced by rev213: it replaces the already-recorded H6 leaf "wire cost-certified exact Design execution back into the logarithmic Design wrapper." Therefore the over-count full-tree rewrite trigger is not claimed to fire in this revision.

## Cross-layer existing-solution audit

The relevant pieces already existed at different layers: rev184 constructs the logarithmic Johnson relation and exact H5 filters; rev190 executes the complete finite Design branch cover; rev191 wires homogeneous H5 into that exact H6 execution but withholds theorem-scale complexity certification; rev192 certifies tuple-pair branch multiplicity; rev212 validates and numerically absorbs the actual proof-carrying child recurrence trees of an exact Design execution.

The parent-level solution is consequently composition, not another Design solver. An exact finite H6 result is promoted to a proof-carrying SI terminal only when rev212 accepts the exact same executed Design result under the original root envelope. If the H6 result is nonexact, if the Design theorem gate fails, or if any branch recurrence/cost certificate is rejected, the wrapper remains unresolved even if some structural evidence exists.

## Direct implementation

`signed_johnson_log_design_lemma_si_v3.py` wraps rev191. It preserves already exact H5 delegated proofs unchanged. Only when rev191 actually reaches an exact H6 Design result does it invoke rev212 with the H5 prefix charge. A successful certificate is converted to one `ProofCarryingCoset` terminal whose accounting node already includes the complete validated branch execution cost. Set correctness comes from rev190/rev191; cost correctness comes from rev192/rev212; neither is substituted for the other.

The real structural regression uses J(9,4) colored by whether a 4-set contains an affine-plane-of-order-3 line. The canonical t=3 logarithmic relation separates exactly the 12 affine lines from the 72 non-lines. The line class is a 2-(9,3,1) design, so simple point-incidence and pair-codegree descent is homogeneous while the relation remains nontrivial. The regression therefore reaches the actual homogeneous Design boundary when resources permit. Constant homogeneous relations remain rejected at the symmetry-defect theorem gate.

## Resulting boundary

After validation, the former gap "rev191 exact H6 Design set but no composed root-scale execution certificate" is removable whenever every executed tuple-branch SI already carries accepted recurrence evidence. Remaining Design work is restricted to instances where rev191 itself is nonexact, or where one of those child proof trees is still unresolved/rejected. Those cases must continue through the strengthened k-WL / Split-or-Johnson / candidate machinery rather than being called solved.

True nonliteral giant quotients, primitive non-Johnson states, unresolved large signed-ground homogeneous Design cases, remaining corrected Split-or-Johnson recursion, global W1R-H6 closure, and strict AGI generality/performance/autonomy/practical-delivery evidence remain open. AGI remains **NOT_AGI**.
