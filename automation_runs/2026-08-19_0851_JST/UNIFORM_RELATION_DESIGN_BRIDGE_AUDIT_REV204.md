# rev204 actual containment-relation -> exact WL/Design bridge audit

Run identity: `2026-08-20T09:24:53+09:00__rev201__43a16b622129`  
Execution start: `2026-08-20T09:24:53+09:00`  
Stacked base: rev203 current-main-line candidate  
AGI-GI transition under test: `rev203 -> rev204`

## Scope and strict status

rev203 resolves the large relation-twin branch of rev202's nonconstant containment relation. rev204 handles the complementary case where rev185's exact symmetry-defect gate is already satisfied.

No caller flag is accepted as evidence of a useful coherent configuration. rev204 consumes the actual paired containment relation produced from the original bipartite incidence by rev202 and the exact symmetry result produced by rev203.

- If relation arity is `1`, the relation itself is an ordered color partition of the right ground. Passing the exact symmetry gate means its largest color/twin class is alpha-bounded, so this is already a canonical paired alpha coloring; k-WL is neither needed nor invoked.
- If relation arity is at least `2`, the complete source/target relation palettes are passed to the existing exact correlated-replacement k-WL / Design implementation (`colored_subset_exact_twl_design_v1.py`) through the complete branch-plan interface (`colored_subset_exact_twl_branch_plan_v1.py`).

Every theorem-parameter gate, symmetry-defect gate, tuple-state cap, work cap, first-successful-level completeness check, and branch-materialization cap remains active. A resource/theorem failure is undetermined, not progress.

`AGI = NOT_AGI`. Ambient parent-group transport, exact parent-string intersection, global recurrence closure, generality/performance/autonomy evidence, and usable AGI delivery are not claimed.

Problem count remains **predicted 512 / effective 512**.

## Existing-world object and cross-layer mapping

The bridge follows the corrected graph-isomorphism proof architecture already encoded in earlier revs:

1. **Bipartite structural layer (rev202).** A twin-free common-degree left cell is normalized into an exact uniform hypergraph and then a theorem-arity containment-count relation on the right ground.
2. **Symmetry branch (rev203/rev185).** Large transposition-twin classes are removed by canonical restriction; otherwise the exact symmetry-defect hypothesis is mechanically certified.
3. **Unary edge case.** A nonconstant 1-ary relation is already a canonical coloring. Treating it directly avoids applying a Design theorem outside its `k>=2` parameter range.
4. **k-WL coherent-refinement layer (rev193).** For arity `>=2`, exact correlated-replacement k-WL is run on the complete colored relation, and the stable 2-skeleton is mechanically checked for coherent-configuration structure constants.
5. **Design output layer.** The implementation exhausts the complete first successful individualization level and accepts only an alpha coloring, an imprimitive alpha partition, or a UPCC branch.
6. **Complete paired branch cover.** rev204 reuses the exact paired witness inventory and complete Cartesian branch-plan machinery; no single label-dependent witness is selected.
7. **Boundary.** The resulting structural branch cover still needs an exact ambient transporter in the parent group and intersection with the complete original incidence/string state.

## Regressions

- A 5-cycle regarded as five distinct degree-two left neighborhoods on a five-point right ground reaches a nonconstant pair containment relation with singleton relation twins; the exact 2-WL/Design path must produce a complete branch plan.
- Two distinct degree-one left neighborhoods on a four-point right ground produce a nonconstant unary relation with a `2+2` alpha-bounded partition; this must terminate structurally without k-WL.
- The rev203 `4+1` large-twin instance must redirect to the canonical proper restriction rather than duplicating Design work.
- A complete 2-uniform family must remain an explicit Johnson-transport obligation.
- A deliberately tiny tuple-state cap must fail closed after relation provenance and symmetry certification.
- A rev202 outcome mismatch must propagate exact empty.

## Problem-tree effect

Subject to repository CI, H6-R3c's structural provenance is covered by exact complementary branches for the current uniform-neighborhood path:

- rev203: failed symmetry gate -> canonical dominant relation-twin proper restriction;
- rev204: passed symmetry gate -> unary alpha coloring or exact k-WL/Design complete branch cover;
- rev202 explicit Johnson outcome -> remains a separate ambient Johnson transport branch.

The next unresolved leaf is **H6-R4 exact ambient paired transport**: lift the explicit Johnson coordinates, canonical right restrictions, unary partitions, and Design witness branches through the parent permutation-group action without losing or inventing isomorphisms. H6-R5 exact full-string integration and H6-C1 recurrence closure remain after that.
