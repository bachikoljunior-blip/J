name: rev238 Design nested intransitive resource smoke

on:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install pytest
      - run: pytest -q automation_runs/2026-08-19_0851_JST/test_design_nested_intransitive_resource_rev238.py
