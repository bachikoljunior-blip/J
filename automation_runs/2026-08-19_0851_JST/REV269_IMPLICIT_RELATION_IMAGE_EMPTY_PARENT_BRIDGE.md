# AGI-GI rev269 — exact-empty auxiliary image to parent-empty semantic bridge

## Scope

Claim: `chatgpt-session-j-rev269-image-empty-parent-bridge-20260822T0845JST-c758f1af`

Exclusive scope: `crx1/bounded-arity-relation-image/implicit-generator-group/exact-empty-image-parent-semantic-bridge`.

This revision closes only the semantic gap explicitly left by rev268: after rev257 has certified the complete faithful named unary/binary auxiliary action, rev262 may prove that the complete value-preserving transporter inside that image is empty, and rev267 may preserve that exact empty result as an empty original-domain preimage. Those two exact-empty artifacts had not yet been promoted to a parent relation-isomorphism empty outcome.

## Exact implication

For every parent relation isomorphism `g` in the supplied original-domain group, rev257's induced action maps `g` to an element of the exact auxiliary image group. The auxiliary feature string is the complete named unary/binary incidence encoding used by rev257, so the induced image of a parent relation isomorphism must carry the complete source feature string to the complete target feature string. Therefore it lies in the value-preserving image transporter computed by rev262.

Consequently, if rev262 certifies that complete transporter set is empty, no parent relation isomorphism exists. Rev267's exact empty original-domain preimage is required as an independent consistency witness before rev269 emits the exact parent-empty outcome.

Faithfulness is rechecked mechanically (`|ker| = 1`, `|image| = |domain group|`, neutral point layer present on both feature strings), although the empty-image implication itself only needs completeness of the induced parent-to-image map.

## Accepted evidence

The bridge accepts only:

- integrated rev257 status `exact_implicit_relation_image_paired_action` with complete domain/image/kernel chains and faithful neutral-layer invariants;
- rev262 structural exact-empty status `exact_empty_feature_inventory_mismatch` or `exact_empty_implicit_image_value_coset`, with `exact=True`, `complete=True`, matching auxiliary degree, and no coset;
- rev267 structural status `exact_empty_original_domain_relation_coset`, with `exact=True`, `complete=True`, matching original/auxiliary degrees, and no representative/subgroup/coset.

Any nonexact, contradictory, wrong-degree, wrong-status, or nonempty artifact fails closed.

The returned value uses the integrated `ParentExactOutcomeContract` shape with status `exact_parent_outcome_empty`, source evidence revision 269, and source status `exact_empty_parent_auxiliary_image_obstruction`. Canonical SHA-256 digests bind the source/target relation identities and both upstream artifact identities.

## Parallel boundary

The implementation does not import rev262 or rev267 branch-only modules and does not modify their PRs or branches. It does not modify any rev268 reserved file. It also does not change `MAIN.md`, shared claim/admission machinery, CRX3 work, or rev247 S1 work.

## Strict boundary

This revision does not compute the rev262 image transporter, construct the rev267 preimage, discharge rev265 original-root resource accounting, or modify the active rev268 production-parent orchestrator. A later collision-free integration may teach the production parent to consume the rev269 exact-empty outcome. CRX1, GI, and AGI remain unresolved. State remains `NOT_AGI`.
