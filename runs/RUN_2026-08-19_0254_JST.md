# AGI root-problem run record — 2026-08-19

Root certification remains **NOT_AGI**.

## State-integrity repair

A stale 95-node `PROBLEM_TREE.json` overwrite was detected because the later `CURRENT_STATUS.json` and mandatory 219→127 consolidation artifacts described a more advanced state. The tree was rebuilt exclusively from current-session local artifacts: the evidence-backed 187-node reconstruction, the subsequent 219-node decomposition state, the mandatory whole-tree consolidation to 127 nodes with prediction reset to 170, and the three post-consolidation identifiability decompositions to 136 nodes. Duplicate total-correlation children from the stale overwrite were rejected rather than re-added; their existing evidence-backed children were retained and their parent integrated. `tree_integrity.py` passes and the root harness has 28 passing tests.

A revision/hash index and immutable tree snapshots were added so later stale overwrites can be detected.

## New leaf attempt

Selected `C2.2b2b2b5b3s2b2b`: dependent learned-density/manifold residual-symmetry transport with certification.

Implemented an exact bounded subcase for known full-rank multivariate Gaussian densities. Cholesky whitening plus Gaussian probability-integral transforms conjugate the existing compact unit-cube volume-preserving residual symmetry into the dependent Gaussian coordinates. A separate empirical holdout gate checks estimated Gaussian transports but is explicitly not treated as an exact certificate.

Focused regression: 3 tests passed. The broad parent remains unresolved and was decomposed into known-Gaussian (solved_v0_1), known non-Gaussian triangular/Rosenblatt transport (unresolved), learned dependent transport with quantitative transport/Jacobian error certificate (unresolved), and manifold probability transport (unresolved).

## Problem counts

After this decomposition: 140 active nodes versus re-predicted budget 170. The mandatory consolidation trigger is not active.

Next leaf: `C2.2b2b2b5b3s2b2b2` — certified triangular/Rosenblatt transport for known dependent non-Gaussian densities.
