# AGI-GI rev280 literal-giant execution proof-DAG consumer

## Selected collision-free child

rev280 claims only `crx3/algorithmic-consumers/literal-giant-exact-terminal-proof-dag`.
It consumes two already-main-integrated substrates: rev208's exact natural-domain
`S_n`/`A_n` string-isomorphism terminal and rev220's conservative execution
proof-DAG validator. It does not modify either shared implementation.

The concurrently active rev275--rev279 scopes, rev273, rev271, rev264, the
state-orbit proof-DAG consumer, rev252, CRX1/CRX2 production work, `MAIN.md`,
and every sibling claim/branch/PR/workflow are explicitly excluded.

## Identity boundary

The consumer freezes the complete deterministic Schreier-chain representation,
its derived literal group type, oriented source/target values, original root,
producer version, local-cost formula identity, and shared proof-DAG schema into
one immutable `LiteralGiantProofIdentity`.

Only these complete exact rev208 statuses may receive the identity:

- `exact_literal_giant_string_isomorphism`;
- `exact_empty_literal_giant_value_multiplicity`;
- `exact_empty_literal_alternating_parity`.

The identity validator independently requires exactness, canonicality, terminal
and local-cost certification, the literal giant operation kind, matching proof
and accounting measures, equal local charges, leaf structure, and consistent
empty/nonempty coset semantics. Hashable but opaque values may still execute in
rev208, but their frozen identity is marked non-replay-stable and therefore
cannot be admitted to shared proof-DAG storage.

## Conservative accounting

After identity validation, rev280 delegates only to the main-integrated rev220
`validate_execution_proof_dag`. The proof-DAG may deduplicate proof storage, but
rev220 still charges every represented execution occurrence and independently
checks the recurrence tree and original-root quasipolynomial envelope. rev280
introduces no cache-based work discount and no relaxed polynomial lift rule.

## Fail-closed behavior

Nonliteral groups, degree-below-five outcomes, nonexact producer results,
malformed/tampered identities, opaque identities, accounting disagreement,
contradictory coset semantics, and exceeded original-root envelopes all remain
uncertified. No unresolved structural result is promoted to exactness.

## Strict boundary

This revision does not change literal-giant recognition, color transport,
Schreier construction, S1 dispatch, corrected Split-or-Johnson, other CRX3
consumers, global String Isomorphism/Graph Isomorphism, or any practical AGI
claim. State remains `NOT_AGI`.
